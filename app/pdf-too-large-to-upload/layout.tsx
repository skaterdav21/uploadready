export const metadata = {
  title: "PDF Too Large to Upload? Fix It Fast | UploadReady",
  description:
    "Make oversized PDFs upload-ready. Compress PDFs, split pages, or rebuild cleaner upload files for resumes, forms, and portals.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
