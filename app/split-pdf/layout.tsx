export const metadata = {
  title: "Split PDF — Extract Pages for Upload",
  description:
    "Split PDF files or extract pages to upload only what a form requires.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
