export const metadata = {
  title: "Resume Upload Fixer — Make Resume Files Ready for Job Applications",
  description:
    "Fix resume upload problems fast. Compress PDFs, merge cover letters, split pages, resize images, and convert HEIC photos for job applications.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
