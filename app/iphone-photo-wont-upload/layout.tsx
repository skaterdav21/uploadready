export const metadata = {
  title: "iPhone Photo Won’t Upload? Fix HEIC and Size Issues | UploadReady",
  description:
    "Fix iPhone photo upload problems fast. Convert HEIC to JPG, resize oversized images, and compress photos for websites and forms.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
